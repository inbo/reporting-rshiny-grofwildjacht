#Libraries
```{r}
library(tidyverse)
library(ggplot2)
```

#Data
```{r}
Aantrekking_data <- read.csv("./dashboard/input/maatschappelijke_draagkracht/F14_2_data.csv",
                         sep = ",",
                         header = TRUE)

```

#Grafiek
```{r}
Aantrekking_data$Antwoord <- factor(Aantrekking_data$Antwoord , 
                                    levels = c('Ja, zeker wel', 'Ja, waarschijnlijk wel', 
                                               'Nee, waarschijnlijk niet', 'Nee, zeker niet'))

plot_aantrekking <- ggplot(data = Aantrekking_data,
                           aes(x = percentage,
                               y = Sector,
                               fill = Antwoord)) +
  geom_bar(stat = "identity", 
           position = "stack") +
  facet_wrap(~ Year)

plot_aantrekking

```


